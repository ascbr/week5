
<br/>
<h1>Shopping Kart</h1>
<div class="panel panel-default">
    
    <div class="panel-body">
    <% if @purchase %>
      <h4>
        Purchase id: <%=@purchase.id%><br/>
        User: <%=@user.username%>
      <h4>
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>Product Details</th>
            <th>Price</th>
            <th>Units</th>
            <th>Total/product</th>
          </tr>
        </thead>
        <% sum_total = 0%>
        <%@orders.each do |o| %>
            <tr>
                <td>
                   <h3>
                     <%= o.product.name %>          
                   </h3>
                   <div class='text-muted'> 
                      Category: <%= o.product.category.name %> <br/>
                      SKU: <%= o.product.sku %> 
                       
                   </div>
                </td>
                <td>
                  $ <%=o.product.price.round(2)%>
                </td>
                <td>
                  <%=o.quantity%>
                </td>
                <td>
                  $ <%=o.product.price.round(2) * o.quantity%>
                    <%sum_total += o.product.price.round(2) * o.quantity%>
                </td>
            </tr>
        <% end %>
      </table>
      <div class='panel panel-danger'>
        <div class='text-right panel-body'>
           <h1> Total: $ <%=sum_total.round(2)%></h1>
        <div>
        <div>
          <%= form_for @purchase, url: purchase_orders_path,  method: :post do |f| %>
            <%= f.hidden_field :purchase_id, value: @purchase.id %>  
            <%= f.hidden_field :total, value: sum_total %>   
            <%= f.submit "Purchase", class:"btn btn-success btn-lg"%>
          <%end%>
            
        </div>
      </div>
      <%else%>
        
         <div class="alert alert-warning" role="alert">
          <h3>No products Added, Return to navigate.</h3>
          <a href="/products" class="btn btn-link btn-lg">Products</a>
           
          </div>          
        
      <%end%>
    </div>
    
</div> 